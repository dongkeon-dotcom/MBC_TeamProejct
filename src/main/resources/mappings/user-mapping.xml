<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="MEMBER">
 
<insert id="INSERT" parameterType="MemberVO">
 insert into member ( user_id,name,tel,email,role, address,password,age,gender,etc) 
 values ( #{user_id},#{name},#{tel},#{email},#{role},#{address},#{password},#{age},#{gender},#{etc} )
</insert>

<select id="SELECT"  resultType ="MemberVO" >
  select  *  from  	member                     
</select>	

<select id="LOGIN" parameterType="MemberVO" resultType ="MemberVO" >
  select  *  from  	member  where  user_id =#{user_id} and  password =#{password}                  
</select>

<select id="EDIT" parameterType="MemberVO" resultType ="MemberVO" >
  select  *  from  	member 
  where  user_id =#{user_id}                  
</select>

</mapper>